<%- include('../../../partials/header', { title: title }) %>
<%- include('../../../partials/navbar', { user: user }) %>

<div class="container mx-auto px-4 py-8">
  <div class="mb-6">
    <h1 class="text-3xl font-bold text-gray-800 mb-2">Marcar Taxa como Paga</h1>
    <p class="text-gray-600">Registre o pagamento da taxa</p>
  </div>

  <div class="bg-white rounded-lg shadow-lg p-6 max-w-2xl">
    <div class="mb-6 p-4 bg-gray-50 rounded-lg">
      <p class="text-sm text-gray-600 mb-1">Apartamento</p>
      <p class="text-lg font-semibold"><%= fee.apartment_number %><%= fee.block ? ' - ' + fee.block : '' %></p>
      
      <% 
        // O campo 'amount' já inclui multa e juros
        // Calcula o valor base subtraindo multa e juros
        const baseAmount = parseFloat(fee.amount) - parseFloat(fee.late_fee || 0) - parseFloat(fee.interest || 0);
        const totalAmount = parseFloat(fee.amount); // Já inclui tudo
        const lateFee = parseFloat(fee.late_fee || 0);
        const interest = parseFloat(fee.interest || 0);
      %>
      
      <div class="mt-4 space-y-2">
        <div class="flex justify-between items-center">
          <p class="text-sm text-gray-600">Valor Base da Taxa:</p>
          <p class="text-sm font-semibold">R$ <%= baseAmount.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></p>
        </div>
        
        <% if (lateFee > 0) { %>
          <div class="flex justify-between items-center">
            <p class="text-sm text-gray-600">Multa:</p>
            <p class="text-sm font-semibold text-orange-600">R$ <%= lateFee.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></p>
          </div>
        <% } %>
        
        <% if (interest > 0) { %>
          <div class="flex justify-between items-center">
            <p class="text-sm text-gray-600">Juros:</p>
            <p class="text-sm font-semibold text-orange-600">R$ <%= interest.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></p>
          </div>
        <% } %>
        
        <div class="border-t border-gray-300 pt-2 mt-2">
          <div class="flex justify-between items-center">
            <p class="text-base font-semibold text-gray-800">Total a Pagar:</p>
            <p class="text-xl font-bold text-red-600">
              R$ <%= totalAmount.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %>
            </p>
          </div>
        </div>
      </div>
    </div>

    <form method="POST" action="/financeiro/taxas/<%= fee.id %>/pagar" enctype="multipart/form-data">
      <div class="space-y-4">
        <div>
          <label for="paymentMethod" class="block text-sm font-medium text-gray-700 mb-2">
            Método de Pagamento <span class="text-red-500">*</span>
          </label>
          <select
            id="paymentMethod"
            name="paymentMethod"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
          >
            <option value="">Selecione...</option>
            <option value="PIX">PIX</option>
            <option value="BOLETO">Boleto</option>
            <option value="TRANSFERENCIA">Transferência Bancária</option>
            <option value="DINHEIRO">Dinheiro</option>
            <option value="CHEQUE">Cheque</option>
            <option value="OUTRA">Outra</option>
          </select>
        </div>

        <div>
          <label for="paymentReceiptPdf" class="block text-sm font-medium text-gray-700 mb-2">
            Comprovante (PDF)
          </label>
          <input
            type="file"
            id="paymentReceiptPdf"
            name="paymentReceiptPdf"
            accept="application/pdf"
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
          <p class="text-xs text-gray-500 mt-1">Apenas arquivos PDF são permitidos (máximo 10MB)</p>
        </div>

        <div class="flex gap-4">
          <button type="submit" class="px-6 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition-colors">
            Marcar como Paga
          </button>
          <a href="/financeiro/taxas" class="px-6 py-2 bg-gray-300 text-gray-700 rounded-lg hover:bg-gray-400 transition-colors">
            Cancelar
          </a>
        </div>
      </div>
    </form>
  </div>
</div>

<%- include('../../../partials/footer') %>
