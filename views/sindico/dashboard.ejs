<%- include('../partials/header', { title: title }) %>
<%- include('../partials/navbar', { user: user }) %>

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  @keyframes pulse {
    0%, 100% {
      opacity: 1;
      transform: scale(1);
    }
    50% {
      opacity: 0.8;
      transform: scale(1.05);
    }
  }
  
  @keyframes shimmer {
    0% {
      background-position: -1000px 0;
    }
    100% {
      background-position: 1000px 0;
    }
  }
  
  .animate-fade-in-up {
    animation: fadeInUp 0.6s ease-out;
  }
  
  .card-elevate {
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  .card-elevate:hover {
    transform: translateY(-4px);
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  }
  
  .stat-card {
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.9) 0%, rgba(255, 255, 255, 0.95) 100%);
    backdrop-filter: blur(10px);
  }
  
  .pulse-animation {
    animation: pulse 2s ease-in-out infinite;
  }
  
  .shimmer-effect {
    background: linear-gradient(90deg, #f0f0f0 0%, #e0e0e0 50%, #f0f0f0 100%);
    background-size: 2000px 100%;
    animation: shimmer 2s infinite;
  }
</style>

<div class="min-h-screen bg-gradient-to-br from-gray-50 via-purple-50/30 to-pink-50/30">
  <div class="container mx-auto px-4 py-8">
    <!-- Cabeçalho Moderno -->
    <div class="mb-8 animate-fade-in-up">
      <div class="flex items-center space-x-4 mb-4">
        <div class="bg-gradient-to-br from-purple-500 to-pink-600 rounded-xl p-4 shadow-lg">
          <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
          </svg>
        </div>
        <div>
          <h1 class="text-4xl font-bold bg-gradient-to-r from-gray-900 via-purple-900 to-gray-900 bg-clip-text text-transparent">
            Dashboard Síndico
          </h1>
          <p class="text-gray-600 mt-1">Visão executiva do condomínio</p>
        </div>
      </div>
    </div>

    <!-- Cards de Estatísticas Principais -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-6">
      <!-- Aprovações Pendentes -->
      <div class="stat-card rounded-2xl shadow-lg p-6 border-l-4 border-yellow-500 card-elevate animate-fade-in-up <%= stats.pendingApprovals > 0 ? 'pulse-animation' : '' %>" style="animation-delay: 0.1s">
        <div class="flex items-center justify-between mb-4">
          <div class="flex-1">
            <p class="text-sm font-semibold text-gray-600 mb-1">Aprovações Pendentes</p>
            <p class="text-4xl font-bold bg-gradient-to-r from-yellow-600 to-amber-600 bg-clip-text text-transparent">
              <%= stats.pendingApprovals %>
            </p>
            <% if (stats.pendingAmount > 0) { %>
              <p class="text-xs text-gray-500 mt-2 font-semibold">R$ <%= stats.pendingAmount.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></p>
            <% } %>
          </div>
          <div class="bg-gradient-to-br from-yellow-500 to-amber-600 rounded-xl p-4 shadow-md">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
        </div>
        <a href="/sindico/aprovacoes" class="inline-flex items-center text-sm font-semibold text-yellow-600 hover:text-yellow-700 transition-colors duration-200 group">
          Ver todas
          <svg class="w-4 h-4 ml-1 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>

      <!-- Alertas Críticos -->
      <div class="stat-card rounded-2xl shadow-lg p-6 border-l-4 border-red-500 card-elevate animate-fade-in-up <%= stats.criticalAlerts > 0 ? 'pulse-animation' : '' %>" style="animation-delay: 0.2s">
        <div class="flex items-center justify-between mb-4">
          <div class="flex-1">
            <p class="text-sm font-semibold text-gray-600 mb-1">Alertas Críticos</p>
            <p class="text-4xl font-bold bg-gradient-to-r from-red-600 to-red-700 bg-clip-text text-transparent">
              <%= stats.criticalAlerts %>
            </p>
          </div>
          <div class="bg-gradient-to-br from-red-500 to-red-600 rounded-xl p-4 shadow-md">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
            </svg>
          </div>
        </div>
        <a href="/sindico/alertas?severity=CRITICAL" class="inline-flex items-center text-sm font-semibold text-red-600 hover:text-red-700 transition-colors duration-200 group">
          Ver todos
          <svg class="w-4 h-4 ml-1 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>

      <!-- Alertas de Aviso -->
      <div class="stat-card rounded-2xl shadow-lg p-6 border-l-4 border-yellow-500 card-elevate animate-fade-in-up" style="animation-delay: 0.3s">
        <div class="flex items-center justify-between mb-4">
          <div class="flex-1">
            <p class="text-sm font-semibold text-gray-600 mb-1">Alertas de Aviso</p>
            <p class="text-4xl font-bold bg-gradient-to-r from-yellow-600 to-amber-600 bg-clip-text text-transparent">
              <%= stats.warningAlerts %>
            </p>
          </div>
          <div class="bg-gradient-to-br from-yellow-500 to-amber-600 rounded-xl p-4 shadow-md">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
            </svg>
          </div>
        </div>
        <a href="/sindico/alertas?severity=WARNING" class="inline-flex items-center text-sm font-semibold text-yellow-600 hover:text-yellow-700 transition-colors duration-200 group">
          Ver todos
          <svg class="w-4 h-4 ml-1 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>

      <!-- Saldo Financeiro -->
      <div class="stat-card rounded-2xl shadow-lg p-6 border-l-4 <%= stats.balance >= 0 ? 'border-green-500' : 'border-red-500' %> card-elevate animate-fade-in-up" style="animation-delay: 0.4s">
        <div class="flex items-center justify-between mb-4">
          <div class="flex-1">
            <p class="text-sm font-semibold text-gray-600 mb-1">Saldo Atual</p>
            <p class="text-4xl font-bold bg-gradient-to-r <%= stats.balance >= 0 ? 'from-green-600 to-emerald-600' : 'from-red-600 to-red-700' %> bg-clip-text text-transparent">
              R$ <%= stats.balance.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %>
            </p>
            <p class="text-xs font-semibold mt-2 <%= stats.balance >= 0 ? 'text-green-600' : 'text-red-600' %>">
              <%= stats.balance >= 0 ? 'Positivo' : 'Negativo' %>
            </p>
          </div>
          <div class="bg-gradient-to-br <%= stats.balance >= 0 ? 'from-green-500 to-emerald-600' : 'from-red-500 to-red-600' %> rounded-xl p-4 shadow-md">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
        </div>
      </div>
    </div>

    <!-- Segunda Linha de Cards - Gastos e Inadimplência -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-6">
      <!-- Gastos do Mês -->
      <div class="stat-card rounded-2xl shadow-lg p-6 border-l-4 border-blue-500 card-elevate animate-fade-in-up" style="animation-delay: 0.5s">
        <div class="flex items-center justify-between mb-4">
          <div class="flex-1">
            <p class="text-sm font-semibold text-gray-600 mb-1">Gastos do Mês</p>
            <p class="text-4xl font-bold bg-gradient-to-r from-blue-600 to-cyan-600 bg-clip-text text-transparent">
              R$ <%= typeof stats.currentMonthExpenses !== 'undefined' ? stats.currentMonthExpenses.toLocaleString('pt-BR', {minimumFractionDigits: 2}) : '0,00' %>
            </p>
            <% if (typeof stats.expensesVariation !== 'undefined' && stats.expensesVariation !== 0) { %>
              <p class="text-xs font-semibold mt-2 <%= stats.expensesVariation > 0 ? 'text-red-600' : 'text-green-600' %>">
                <%= stats.expensesVariation > 0 ? '+' : '' %><%= stats.expensesVariation.toFixed(1) %>% vs mês anterior
              </p>
            <% } %>
          </div>
          <div class="bg-gradient-to-br from-blue-500 to-cyan-600 rounded-xl p-4 shadow-md">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
            </svg>
          </div>
        </div>
        <a href="/financeiro/saidas" class="inline-flex items-center text-sm font-semibold text-blue-600 hover:text-blue-700 transition-colors duration-200 group">
          Ver detalhes
          <svg class="w-4 h-4 ml-1 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>

      <!-- Inadimplência -->
      <div class="stat-card rounded-2xl shadow-lg p-6 border-l-4 <%= typeof stats.delinquencyRate !== 'undefined' && stats.delinquencyRate > 10 ? 'border-red-500' : typeof stats.delinquencyRate !== 'undefined' && stats.delinquencyRate > 5 ? 'border-yellow-500' : 'border-green-500' %> card-elevate animate-fade-in-up" style="animation-delay: 0.6s">
        <div class="flex items-center justify-between mb-4">
          <div class="flex-1">
            <p class="text-sm font-semibold text-gray-600 mb-1">Inadimplência</p>
            <p class="text-4xl font-bold bg-gradient-to-r <%= typeof stats.delinquencyRate !== 'undefined' && stats.delinquencyRate > 10 ? 'from-red-600 to-red-700' : typeof stats.delinquencyRate !== 'undefined' && stats.delinquencyRate > 5 ? 'from-yellow-600 to-amber-600' : 'from-green-600 to-emerald-600' %> bg-clip-text text-transparent">
              <%= typeof stats.delinquencyRate !== 'undefined' ? stats.delinquencyRate.toFixed(1) : '0,0' %>%
            </p>
            <% if (typeof stats.totalOverdue !== 'undefined' && stats.totalOverdue > 0) { %>
              <p class="text-xs text-gray-500 mt-2 font-semibold">
                R$ <%= stats.totalOverdue.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %> em aberto
              </p>
            <% } %>
            <% if (typeof stats.overdueCount !== 'undefined' && stats.overdueCount > 0) { %>
              <p class="text-xs text-gray-500 mt-1">
                <%= stats.overdueCount %> apartamento(s) inadimplente(s)
              </p>
            <% } %>
          </div>
          <div class="bg-gradient-to-br <%= typeof stats.delinquencyRate !== 'undefined' && stats.delinquencyRate > 10 ? 'from-red-500 to-red-600' : typeof stats.delinquencyRate !== 'undefined' && stats.delinquencyRate > 5 ? 'from-yellow-500 to-amber-600' : 'from-green-500 to-emerald-600' %> rounded-xl p-4 shadow-md">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
        </div>
      </div>
    </div>

    <!-- Segunda Linha de Cards -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-6">
      <!-- Despesas Pendentes -->
      <div class="stat-card rounded-2xl shadow-lg p-6 border-l-4 border-blue-500 card-elevate animate-fade-in-up" style="animation-delay: 0.5s">
        <div class="flex items-center justify-between mb-4">
          <div class="flex-1">
            <p class="text-sm font-semibold text-gray-600 mb-1">Despesas Pendentes</p>
            <p class="text-4xl font-bold bg-gradient-to-r from-blue-600 to-blue-700 bg-clip-text text-transparent">
              <%= stats.pendingExpenses || 0 %>
            </p>
          </div>
          <div class="bg-gradient-to-br from-blue-500 to-blue-600 rounded-xl p-4 shadow-md">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
        </div>
        <a href="/sindico/saidas-pendentes" class="inline-flex items-center text-sm font-semibold text-blue-600 hover:text-blue-700 transition-colors duration-200 group">
          Ver saídas pendentes
          <svg class="w-4 h-4 ml-1 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>

      <!-- Tarefas Atrasadas -->
      <div class="stat-card rounded-2xl shadow-lg p-6 border-l-4 border-red-500 card-elevate animate-fade-in-up <%= stats.overdueTasks > 0 ? 'pulse-animation' : '' %>" style="animation-delay: 0.6s">
        <div class="flex items-center justify-between mb-4">
          <div class="flex-1">
            <p class="text-sm font-semibold text-gray-600 mb-1">Tarefas Atrasadas</p>
            <p class="text-4xl font-bold bg-gradient-to-r from-red-600 to-red-700 bg-clip-text text-transparent">
              <%= stats.overdueTasks %>
            </p>
          </div>
          <div class="bg-gradient-to-br from-red-500 to-red-600 rounded-xl p-4 shadow-md">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
        </div>
        <a href="/sindico/tarefas" class="inline-flex items-center text-sm font-semibold text-red-600 hover:text-red-700 transition-colors duration-200 group">
          Ver todas as tarefas
          <svg class="w-4 h-4 ml-1 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>

      <!-- Ocorrências Abertas -->
      <div class="stat-card rounded-2xl shadow-lg p-6 border-l-4 border-blue-500 card-elevate animate-fade-in-up" style="animation-delay: 0.7s">
        <div class="flex items-center justify-between mb-4">
          <div class="flex-1">
            <p class="text-sm font-semibold text-gray-600 mb-1">Ocorrências Abertas</p>
            <p class="text-4xl font-bold bg-gradient-to-r from-blue-600 to-blue-700 bg-clip-text text-transparent">
              <%= stats.openOccurrences %>
            </p>
          </div>
          <div class="bg-gradient-to-br from-blue-500 to-blue-600 rounded-xl p-4 shadow-md">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
          </div>
        </div>
        <a href="/sindico/ocorrencias" class="inline-flex items-center text-sm font-semibold text-blue-600 hover:text-blue-700 transition-colors duration-200 group">
          Ver todas as ocorrências
          <svg class="w-4 h-4 ml-1 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>

      <!-- Orçamentos Pendentes -->
      <div class="stat-card rounded-2xl shadow-lg p-6 border-l-4 border-purple-500 card-elevate animate-fade-in-up" style="animation-delay: 0.8s">
        <div class="flex items-center justify-between mb-4">
          <div class="flex-1">
            <p class="text-sm font-semibold text-gray-600 mb-1">Orçamentos Pendentes</p>
            <p class="text-4xl font-bold bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
              <%= stats.pendingBudgets || 0 %>
            </p>
          </div>
          <div class="bg-gradient-to-br from-purple-500 to-pink-600 rounded-xl p-4 shadow-md">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
          </div>
        </div>
        <a href="/sindico/orcamentos-pendentes" class="inline-flex items-center text-sm font-semibold text-purple-600 hover:text-purple-700 transition-colors duration-200 group">
          Ver orçamentos
          <svg class="w-4 h-4 ml-1 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>
    </div>

    <!-- Resumo Financeiro -->
    <div class="bg-white rounded-2xl shadow-xl p-6 mb-6 animate-fade-in-up" style="animation-delay: 0.9s">
      <div class="flex items-center space-x-3 mb-4">
        <div class="bg-gradient-to-br from-green-500 to-emerald-600 rounded-lg p-2">
          <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
          </svg>
        </div>
        <h2 class="text-xl font-bold text-gray-900">Resumo Financeiro</h2>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div class="bg-gradient-to-r from-green-50 to-emerald-50 rounded-xl p-4 border-l-4 border-green-500">
          <p class="text-xs font-semibold text-gray-600 mb-1">Entradas</p>
          <p class="text-2xl font-bold text-green-700">R$ <%= stats.totalEntries.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></p>
          <% if (typeof analytics !== 'undefined' && analytics.comparison && analytics.comparison.variations.entries !== 0) { %>
            <p class="text-xs mt-2 <%= analytics.comparison.variations.entries >= 0 ? 'text-green-600' : 'text-red-600' %>">
              <%= analytics.comparison.variations.entries >= 0 ? '↑' : '↓' %> <%= Math.abs(analytics.comparison.variations.entries).toFixed(1) %>% vs mês anterior
            </p>
          <% } %>
        </div>
        <div class="bg-gradient-to-r from-red-50 to-rose-50 rounded-xl p-4 border-l-4 border-red-500">
          <p class="text-xs font-semibold text-gray-600 mb-1">Saídas Pagas</p>
          <p class="text-2xl font-bold text-red-700">R$ <%= stats.totalExitsPaid.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></p>
          <% if (typeof analytics !== 'undefined' && analytics.comparison && analytics.comparison.variations.exits !== 0) { %>
            <p class="text-xs mt-2 <%= analytics.comparison.variations.exits >= 0 ? 'text-red-600' : 'text-green-600' %>">
              <%= analytics.comparison.variations.exits >= 0 ? '↑' : '↓' %> <%= Math.abs(analytics.comparison.variations.exits).toFixed(1) %>% vs mês anterior
            </p>
          <% } %>
        </div>
        <div class="bg-gradient-to-r <%= stats.balance >= 0 ? 'from-green-50 to-emerald-50' : 'from-red-50 to-rose-50' %> rounded-xl p-4 border-l-4 <%= stats.balance >= 0 ? 'border-green-500' : 'border-red-500' %>">
          <p class="text-xs font-semibold text-gray-600 mb-1">Saldo</p>
          <p class="text-2xl font-bold <%= stats.balance >= 0 ? 'text-green-700' : 'text-red-700' %>">
            R$ <%= stats.balance.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %>
          </p>
          <% if (typeof analytics !== 'undefined' && analytics.trend) { %>
            <p class="text-xs mt-2 <%= analytics.trend.trend === 'GROWING' ? 'text-green-600' : analytics.trend.trend === 'DECLINING' ? 'text-red-600' : 'text-gray-600' %>">
              <% if (analytics.trend.trend === 'GROWING') { %>📈 Crescendo<% } else if (analytics.trend.trend === 'DECLINING') { %>📉 Declinando<% } else { %>➡️ Estável<% } %>
              (<%= analytics.trend.rate.toFixed(1) %>%)
            </p>
          <% } %>
        </div>
      </div>
    </div>

    <!-- Gráficos Avançados -->
    <% if (typeof analytics !== 'undefined' && analytics.historical && analytics.historical.length > 0) { %>
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-6">
      <!-- Gráfico de Evolução Histórica (12 meses) -->
      <div class="bg-white rounded-2xl shadow-xl p-6 animate-fade-in-up" style="animation-delay: 1.1s">
        <div class="flex items-center justify-between mb-6">
          <h2 class="text-xl font-bold text-gray-900">Evolução Financeira (12 meses)</h2>
          <span class="text-xs text-gray-500 bg-gray-100 px-3 py-1 rounded-full font-medium">Histórico</span>
        </div>
        <div class="chart-container" style="position: relative; height: 300px;">
          <canvas id="historicalChart"></canvas>
        </div>
      </div>

      <!-- Gráfico de Categorias -->
      <% if (typeof analytics.categoryData !== 'undefined' && analytics.categoryData.exits.length > 0) { %>
      <div class="bg-white rounded-2xl shadow-xl p-6 animate-fade-in-up" style="animation-delay: 1.2s">
        <div class="flex items-center justify-between mb-6">
          <h2 class="text-xl font-bold text-gray-900">Gastos por Categoria</h2>
          <span class="text-xs text-gray-500 bg-gray-100 px-3 py-1 rounded-full font-medium">Últimos 6 meses</span>
        </div>
        <div class="chart-container" style="position: relative; height: 300px;">
          <canvas id="categoryChart"></canvas>
        </div>
      </div>
      <% } %>
    </div>

    <!-- Previsões e Comparações -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-6">
      <!-- Previsões para Próximos 3 Meses -->
      <% if (typeof analytics.projections !== 'undefined' && analytics.projections.length > 0) { %>
      <div class="bg-white rounded-2xl shadow-xl p-6 animate-fade-in-up" style="animation-delay: 1.3s">
        <div class="flex items-center justify-between mb-6">
          <h2 class="text-xl font-bold text-gray-900">Previsões Financeiras</h2>
          <span class="text-xs text-gray-500 bg-gray-100 px-3 py-1 rounded-full font-medium">Próximos 3 meses</span>
        </div>
        <div class="space-y-4">
          <% analytics.projections.forEach((proj, index) => { %>
          <div class="border-l-4 <%= proj.projectedBalance >= 0 ? 'border-green-500' : 'border-red-500' %> bg-<%= proj.projectedBalance >= 0 ? 'green' : 'red' %>-50 p-4 rounded-r-lg">
            <div class="flex items-center justify-between mb-2">
              <h3 class="font-semibold text-gray-900"><%= proj.label %></h3>
              <span class="text-sm font-bold <%= proj.projectedBalance >= 0 ? 'text-green-700' : 'text-red-700' %>">
                R$ <%= proj.projectedBalance.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %>
              </span>
            </div>
            <div class="grid grid-cols-2 gap-2 text-xs">
              <div>
                <span class="text-gray-600">Entradas:</span>
                <span class="font-semibold text-green-700 ml-1">R$ <%= proj.projectedEntries.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></span>
              </div>
              <div>
                <span class="text-gray-600">Saídas:</span>
                <span class="font-semibold text-red-700 ml-1">R$ <%= proj.projectedExits.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></span>
              </div>
            </div>
          </div>
          <% }); %>
        </div>
      </div>
      <% } %>

      <!-- Comparação com Mês Anterior -->
      <% if (typeof analytics.comparison !== 'undefined') { %>
      <div class="bg-white rounded-2xl shadow-xl p-6 animate-fade-in-up" style="animation-delay: 1.4s">
        <div class="flex items-center justify-between mb-6">
          <h2 class="text-xl font-bold text-gray-900">Comparação Mensal</h2>
          <span class="text-xs text-gray-500 bg-gray-100 px-3 py-1 rounded-full font-medium">Mês atual vs anterior</span>
        </div>
        <div class="space-y-4">
          <div class="bg-gradient-to-r from-green-50 to-emerald-50 rounded-lg p-4 border-l-4 border-green-500">
            <p class="text-sm font-semibold text-gray-700 mb-2">Entradas</p>
            <div class="flex items-center justify-between">
              <span class="text-lg font-bold text-green-700">
                R$ <%= analytics.comparison.period2.entries.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %>
              </span>
              <span class="text-xs px-2 py-1 rounded-full <%= analytics.comparison.variations.entries >= 0 ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-700' %>">
                <%= analytics.comparison.variations.entries >= 0 ? '↑' : '↓' %> <%= Math.abs(analytics.comparison.variations.entries).toFixed(1) %>%
              </span>
            </div>
            <p class="text-xs text-gray-500 mt-1">Mês anterior: R$ <%= analytics.comparison.period1.entries.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></p>
          </div>
          
          <div class="bg-gradient-to-r from-red-50 to-rose-50 rounded-lg p-4 border-l-4 border-red-500">
            <p class="text-sm font-semibold text-gray-700 mb-2">Saídas</p>
            <div class="flex items-center justify-between">
              <span class="text-lg font-bold text-red-700">
                R$ <%= analytics.comparison.period2.exits.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %>
              </span>
              <span class="text-xs px-2 py-1 rounded-full <%= analytics.comparison.variations.exits >= 0 ? 'bg-red-100 text-red-700' : 'bg-green-100 text-green-700' %>">
                <%= analytics.comparison.variations.exits >= 0 ? '↑' : '↓' %> <%= Math.abs(analytics.comparison.variations.exits).toFixed(1) %>%
              </span>
            </div>
            <p class="text-xs text-gray-500 mt-1">Mês anterior: R$ <%= analytics.comparison.period1.exits.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></p>
          </div>

          <div class="bg-gradient-to-r <%= analytics.comparison.period2.balance >= 0 ? 'from-green-50 to-emerald-50' : 'from-red-50 to-rose-50' %> rounded-lg p-4 border-l-4 <%= analytics.comparison.period2.balance >= 0 ? 'border-green-500' : 'border-red-500' %>">
            <p class="text-sm font-semibold text-gray-700 mb-2">Saldo</p>
            <div class="flex items-center justify-between">
              <span class="text-lg font-bold <%= analytics.comparison.period2.balance >= 0 ? 'text-green-700' : 'text-red-700' %>">
                R$ <%= analytics.comparison.period2.balance.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %>
              </span>
              <span class="text-xs px-2 py-1 rounded-full <%= analytics.comparison.variations.balance >= 0 ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-700' %>">
                <%= analytics.comparison.variations.balance >= 0 ? '↑' : '↓' %> <%= Math.abs(analytics.comparison.variations.balance).toFixed(1) %>%
              </span>
            </div>
            <p class="text-xs text-gray-500 mt-1">Mês anterior: R$ <%= analytics.comparison.period1.balance.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></p>
          </div>
        </div>
      </div>
      <% } %>
    </div>
    <% } %>

    <!-- Ações Rápidas -->
    <div class="bg-white rounded-2xl shadow-xl p-8 animate-fade-in-up" style="animation-delay: 1s">
      <div class="flex items-center space-x-3 mb-6">
        <div class="bg-gradient-to-br from-purple-500 to-pink-600 rounded-lg p-2">
          <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
          </svg>
        </div>
        <h2 class="text-2xl font-bold text-gray-900">Ações Rápidas</h2>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">
        <a href="/sindico/tarefas" class="group relative overflow-hidden bg-gradient-to-r from-purple-600 to-pink-600 text-white px-6 py-4 rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105 flex items-center justify-center space-x-2">
          <span class="absolute inset-0 bg-white opacity-0 group-hover:opacity-20 transition-opacity duration-300"></span>
          <svg class="w-5 h-5 relative z-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
          </svg>
          <span class="relative z-10">Ver Tarefas</span>
        </a>
        
        <a href="/sindico/ocorrencias" class="group relative overflow-hidden bg-gradient-to-r from-blue-600 to-blue-700 text-white px-6 py-4 rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105 flex items-center justify-center space-x-2">
          <span class="absolute inset-0 bg-white opacity-0 group-hover:opacity-20 transition-opacity duration-300"></span>
          <svg class="w-5 h-5 relative z-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
          </svg>
          <span class="relative z-10">Ver Ocorrências</span>
        </a>
        
        <a href="/sindico/aprovacoes" class="group relative overflow-hidden bg-gradient-to-r from-yellow-600 to-amber-600 text-white px-6 py-4 rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105 flex items-center justify-center space-x-2">
          <span class="absolute inset-0 bg-white opacity-0 group-hover:opacity-20 transition-opacity duration-300"></span>
          <svg class="w-5 h-5 relative z-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span class="relative z-10">Revisar Aprovações</span>
        </a>
        
        <a href="/sindico/alertas" class="group relative overflow-hidden bg-gradient-to-r from-red-600 to-red-700 text-white px-6 py-4 rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105 flex items-center justify-center space-x-2">
          <span class="absolute inset-0 bg-white opacity-0 group-hover:opacity-20 transition-opacity duration-300"></span>
          <svg class="w-5 h-5 relative z-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
          </svg>
          <span class="relative z-10">Ver Alertas</span>
        </a>
        
        <a href="/sindico/logs" class="group relative overflow-hidden bg-gradient-to-r from-gray-600 to-gray-700 text-white px-6 py-4 rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105 flex items-center justify-center space-x-2">
          <span class="absolute inset-0 bg-white opacity-0 group-hover:opacity-20 transition-opacity duration-300"></span>
          <svg class="w-5 h-5 relative z-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
          </svg>
          <span class="relative z-10">Logs de Auditoria</span>
        </a>
        
        <a href="/sindico/entradas-pendentes" class="group relative overflow-hidden bg-gradient-to-r from-yellow-600 to-amber-600 text-white px-6 py-4 rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105 flex items-center justify-center space-x-2">
          <span class="absolute inset-0 bg-white opacity-0 group-hover:opacity-20 transition-opacity duration-300"></span>
          <svg class="w-5 h-5 relative z-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span class="relative z-10">Analisar Entradas<% if (stats.pendingEntries !== undefined && stats.pendingEntries > 0) { %> (<%= stats.pendingEntries %>)<% } %></span>
        </a>
        
        <a href="/sindico/saidas-pendentes" class="group relative overflow-hidden bg-gradient-to-r from-red-600 to-red-700 text-white px-6 py-4 rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105 flex items-center justify-center space-x-2">
          <span class="absolute inset-0 bg-white opacity-0 group-hover:opacity-20 transition-opacity duration-300"></span>
          <svg class="w-5 h-5 relative z-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span class="relative z-10">Aprovar Saídas<% if (stats.pendingExpenses !== undefined && stats.pendingExpenses > 0) { %> (<%= stats.pendingExpenses %>)<% } %></span>
        </a>
        
        <% if (stats.pendingOccurrencesApproval !== undefined && stats.pendingOccurrencesApproval > 0) { %>
        <a href="/sindico/ocorrencias-pendentes-aprovacao" class="group relative overflow-hidden bg-gradient-to-r from-blue-600 to-blue-700 text-white px-6 py-4 rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105 flex items-center justify-center space-x-2">
          <span class="absolute inset-0 bg-white opacity-0 group-hover:opacity-20 transition-opacity duration-300"></span>
          <svg class="w-5 h-5 relative z-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span class="relative z-10">Aprovar Ocorrências (<%= stats.pendingOccurrencesApproval %>)</span>
        </a>
        <% } %>
        
        <a href="/sindico/manutencoes" class="group relative overflow-hidden bg-gradient-to-r from-indigo-600 to-indigo-700 text-white px-6 py-4 rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300 transform hover:scale-105 flex items-center justify-center space-x-2">
          <span class="absolute inset-0 bg-white opacity-0 group-hover:opacity-20 transition-opacity duration-300"></span>
          <svg class="w-5 h-5 relative z-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
          </svg>
          <span class="relative z-10">Manutenções</span>
        </a>
      </div>
    </div>
  </div>
</div>

<!-- Chart.js CDN -->
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>

<script>
  <% if (typeof analytics !== 'undefined' && analytics.historical && analytics.historical.length > 0) { %>
  // Gráfico de Evolução Histórica
  const historicalCtx = document.getElementById('historicalChart');
  if (historicalCtx) {
    const historicalData = <%- JSON.stringify(analytics.historical) %>;
    
    new Chart(historicalCtx, {
      type: 'line',
      data: {
        labels: historicalData.map(d => d.label),
        datasets: [
          {
            label: 'Entradas',
            data: historicalData.map(d => parseFloat(d.entries || 0)),
            borderColor: 'rgb(34, 197, 94)',
            backgroundColor: 'rgba(34, 197, 94, 0.1)',
            borderWidth: 3,
            tension: 0.4,
            fill: true,
            pointRadius: 5,
            pointHoverRadius: 7
          },
          {
            label: 'Saídas',
            data: historicalData.map(d => parseFloat(d.exits || 0)),
            borderColor: 'rgb(239, 68, 68)',
            backgroundColor: 'rgba(239, 68, 68, 0.1)',
            borderWidth: 3,
            tension: 0.4,
            fill: true,
            pointRadius: 5,
            pointHoverRadius: 7
          },
          {
            label: 'Saldo',
            data: historicalData.map(d => parseFloat(d.balance || 0)),
            borderColor: 'rgb(59, 130, 246)',
            backgroundColor: 'rgba(59, 130, 246, 0.1)',
            borderWidth: 3,
            tension: 0.4,
            fill: true,
            pointRadius: 5,
            pointHoverRadius: 7
          }
        ]
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: {
            position: 'top',
            labels: {
              usePointStyle: true,
              padding: 15,
              font: { size: 12, weight: 'bold' }
            }
          },
          tooltip: {
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            padding: 15,
            cornerRadius: 12,
            callbacks: {
              label: function(context) {
                return context.dataset.label + ': R$ ' + context.parsed.y.toLocaleString('pt-BR', {minimumFractionDigits: 2});
              }
            }
          }
        },
        scales: {
          y: {
            beginAtZero: false,
            ticks: {
              callback: function(value) {
                return 'R$ ' + value.toLocaleString('pt-BR', {minimumFractionDigits: 2});
              }
            }
          }
        },
        animation: {
          duration: 2000,
          easing: 'easeInOutQuart'
        }
      }
    });
  }
  <% } %>

  <% if (typeof analytics !== 'undefined' && analytics.categoryData && analytics.categoryData.exits.length > 0) { %>
  // Gráfico de Categorias
  const categoryCtx = document.getElementById('categoryChart');
  if (categoryCtx) {
    const categoryData = <%- JSON.stringify(analytics.categoryData.exits) %>;
    
    const colors = [
      'rgba(239, 68, 68, 0.8)',
      'rgba(59, 130, 246, 0.8)',
      'rgba(139, 92, 246, 0.8)',
      'rgba(251, 191, 36, 0.8)',
      'rgba(34, 197, 94, 0.8)',
      'rgba(236, 72, 153, 0.8)'
    ];

    new Chart(categoryCtx, {
      type: 'doughnut',
      data: {
        labels: categoryData.map(d => d.category),
        datasets: [{
          label: 'Gastos por Categoria',
          data: categoryData.map(d => parseFloat(d.total || 0)),
          backgroundColor: categoryData.map((d, i) => colors[i % colors.length]),
          borderWidth: 3,
          borderColor: '#ffffff'
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: {
            position: 'right',
            labels: {
              padding: 15,
              font: { size: 11, weight: 'bold' }
            }
          },
          tooltip: {
            backgroundColor: 'rgba(0, 0, 0, 0.8)',
            padding: 15,
            cornerRadius: 12,
            callbacks: {
              label: function(context) {
                const total = context.dataset.data.reduce((a, b) => a + b, 0);
                const percent = ((context.parsed / total) * 100).toFixed(1);
                return context.label + ': R$ ' + context.parsed.toLocaleString('pt-BR', {minimumFractionDigits: 2}) + ' (' + percent + '%)';
              }
            }
          }
        },
        animation: {
          duration: 2000,
          easing: 'easeInOutQuart'
        }
      }
    });
  }
  <% } %>
</script>

<%- include('../partials/footer') %>
